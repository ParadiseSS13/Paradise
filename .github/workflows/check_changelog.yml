name: Проверка Ченджлога

permissions:
  contents: read
  pull-requests: write
  issues: write

on:
  pull_request_target:
    types: [opened, reopened, edited, labeled, unlabeled]

jobs:
  CheckCL:
    runs-on: ubuntu-latest
    if: github.repository == 'ss220club/Paradise-Remake' && github.base_ref == 'master' && github.event.pull_request.draft == false
    steps:
      - name: Скачивание файлов
        run: |
          wget https://raw.githubusercontent.com/ss220club/Paradise-Remake/master/tools/changelog/sierra_check_changelog.py
          wget https://raw.githubusercontent.com/ss220club/Paradise-Remake/master/tools/changelog/tags.yml
      - name: Установка Python
        uses: actions/setup-python@61a6322f88396a6271a6ee3565807d608ecaddd1
        with:
          python-version: '3.x'
      - name: Установка зависимостей
        run: |
          python -m pip install --upgrade pip
          pip install ruamel.yaml PyGithub
      - name: Проверка чейнджлога
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
        run: python sierra_check_changelog.py
